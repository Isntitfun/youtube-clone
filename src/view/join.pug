extends base

block content 

    h1.banner Join Wetube
    if pwNotConfirmed 
        form(method="POST").general-form
            input(value=`${email}` name="email" placeholder="Email" type="text" required) 
            input(value=`${username}` name="username" placeholder="Username" type="text" required) 
            input(value=`${password}` name="password" placeholder="Password" type="text" required) 
            input(name="password2" placeholder="Confirm Password" type="text" required) 
            input(value=`${name}` name="name" placeholder="Name" type="text" required) 
            input(value=`${location}` name="location" placeholder="Address" type="text" required)
            input(type="submit" value="Join") 
            p If you already have an account, please  
                a(href="/login") Log in &rarr;

    else if notUniqueEmail 
        form(method="POST").general-form
            input(name="email" placeholder="Email" type="text" required) 
            input(value=`${username}` name="username" placeholder="Username" type="text" required) 
            input(value=`${password}` name="password" placeholder="Password" type="text" required) 
            input(name="password2" placeholder="Confirm Password" type="text" required) 
            input(value=`${name}` name="name" placeholder="Name" type="text" required) 
            input(value=`${location}` name="location" placeholder="Address" type="text" required)
            input(type="submit" value="Join") 
            p If you already have an account, please  
                a(href="/login") Log in &rarr;

    else if notUniqueUsername 
        form(method="POST").general-form
            input(value=`${email}` name="email" placeholder="Email" type="text" required) 
            input(name="username" placeholder="Username" type="text" required) 
            input(value=`${password}` name="password" placeholder="Password" type="text" required) 
            input(name="password2" placeholder="Confirm Password" type="text" required) 
            input(value=`${name}` name="name" placeholder="Name" type="text" required) 
            input(value=`${location}` name="location" placeholder="Address" type="text" required)
            input(type="submit" value="Join") 
            p If you already have an account, please  
                a(href="/login") Log in &rarr;

            
    else 
        form(method="POST").general-form
            input(name="email" placeholder="Email" type="text" required) 
            input(name="username" placeholder="Username" type="text" required) 
            input(name="password" placeholder="Password" type="text" required) 
            input(name="password2" placeholder="Confirm Password" type="text" required) 
            input(name="name" placeholder="Name" type="text" required) 
            input(name="location" placeholder="Address" type="text" required)
            input(type="submit" value="Join")
            p If you already have an account, please  
                a(href="/login") Log in &rarr;
 
