extends ../base

block content 

        if errorMessage 
            h5=errorMessage
        
        h1.banner Change Password
        form(method="POST").general-form
            input(name="oldPW" placeholder="Currernt Password" type="password" required) 
            input(name="newPW" placeholder="New Password" type="password" required) 
            input(name="confirmPW" placeholder="Confirm New Password" type="password" required) 
            input(type="submit" value="Change")